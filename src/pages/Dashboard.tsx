import Typography  from "@mui/material/Typography"

const Dashboard = () => {
    return (
        <Typography variant="h4" component="h1" sx={{fontWeight: "bold"}}>Dashboard</Typography>
    )
}

export default Dashboard